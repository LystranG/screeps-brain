---
context: default
phase: post-milestone spawn queue stuck investigation
task: 3
total_tasks: 6
status: in_progress
last_updated: 2026-05-08T09:17:58.626Z
---

# BLOCKING CONSTRAINTS - Read Before Anything Else

> These are not suggestions. Each constraint below was discovered through failure.
> Acknowledge each one explicitly before proceeding.

- [ ] CONSTRAINT: queued spawn request selection gap - a ready colony with one idle spawn and successful `cmd.spawn.dryRun("sim")` can still leave a bootstrap request stuck at `queued` with `attempts=0` and `lastError=none` - inspect queue eligibility before assuming runtime health

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|----------------------|
| dry-run != queue progression | `cmd.spawn.dryRun("sim")` can succeed while `runSpawnLifecycle()` still never selects the queued bootstrap request. | blocking | Start the next session by tracing `selectNextSpawnRequestByStatus()` / `isUsableSpawnCandidate()` rather than trusting the command output. |
| spawnQueue history vs execution state | `spawned` records are retained intentionally and are not the same thing as a request actively being re-queued. | advisory | Distinguish terminal history from live candidate selection when debugging. |

<current_state>
Milestone v1.0 is complete and its verification artifacts are in place, but a new post-milestone runtime issue was observed in the live sim: colony is ready, spawn is idle, guidance is none, `cmd.spawn.dryRun("sim")` succeeds, yet a bootstrap request stays queued forever. This handoff is for diagnosing that selection gap, not for redoing the completed milestone verification.
</current_state>

<completed_work>

Completed Tasks:
- Task 1: Added Verification Matrix documentation and real sim UAT checklist - Done
- Task 2: Added explicit spawn lifecycle and replenishment regression coverage - Done
- Task 3: Observed and isolated the next bug - In progress, queued bootstrap request stuck despite ready colony and idle spawn
</completed_work>

<remaining_work>

- Task 4: Diagnose why the queued bootstrap request is not selected for dry-run validation
- Task 5: Fix the queue selection / eligibility logic if the queue should advance in ready sim conditions
- Task 6: Re-run unit, integration, and sim UAT evidence after the fix
</remaining_work>

<decisions_made>

- Decided to preserve the existing post-milestone documentation and regression work as complete.
- Chose to hand off the new bug through the GSD pause/resume workflow so the next session can resume with structured context.
- Chose not to change runtime code before the handoff so the next investigation starts from the exact observed state.
</decisions_made>

<blockers>
- A bootstrap request can remain queued forever in a ready colony even though the spawn is idle and `cmd.spawn.dryRun("sim")` returns OK. The next session must inspect the queue selection gate, not the command surface alone.
</blockers>

## Required Reading (in order)
1. `.planning/HANDOFF.json` — machine-readable handoff context
1. `src/spawning/queue.ts` — queue candidate selection and status mutation
1. `src/spawning/runner.ts` — lifecycle order and spawn progression
1. `src/bootstrap/spawnDemand.ts` — slot demand generation and duplicate handling
1. `src/commands/namespaces/spawn.ts` — command output / dry-run comparison surface
1. `docs/operations.zh-CN.md` — accepted manual UAT checklist and verification matrix context

## Critical Anti-Patterns (do NOT repeat these)
- Treating `cmd.spawn.dryRun("sim")` success as proof that the queued request will be selected next. The runtime bug showed those are separate gates.

## Infrastructure State
- No dev server or watcher is intentionally running.
- Latest relevant commits: `b77b0f5` and `866aa8e`.
- The worktree is clean before the handoff commit.

<context>
The next investigation should start from the observation that the colony is healthy but the second bootstrap request never leaves `queued`. The most likely path is a queue-selection or eligibility bug around `spawnCapacity` / request filtering, not a general colony readiness problem.
</context>

<next_action>
Start with: run `$gsd-debug` in a new session and trace why `selectNextSpawnRequestByStatus()` / `isUsableSpawnCandidate()` do not advance the queued bootstrap request in an otherwise ready colony.
</next_action>

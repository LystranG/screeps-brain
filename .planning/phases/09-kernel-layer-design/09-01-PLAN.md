---
phase: 09-kernel-layer-design
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/v2-design/KERN-01-highcommand-singleton.md
autonomous: true
requirements:
  - KERN-01

must_haves:
  truths:
    - "KERN-01-highcommand-singleton.md exists in docs/v2-design/"
    - "Document defines HighCommand class with all fields typed as interfaces (IIntelProvider, IGarrison[], ITaskForce), not concrete classes"
    - "Document shows Kernel integration: private highCommand field + highCommandTick stage calling this.highCommand.tick(services)"
    - "Document covers D-01 through D-06 locked decisions with explicit references"
    - "All TypeScript code blocks are SPEC-02 compliant: explicit access modifiers, Chinese JSDoc, I-prefix interfaces"
    - "Document explains global reset detection and build vs refresh branching in tick()"
  artifacts:
    - path: "docs/v2-design/KERN-01-highcommand-singleton.md"
      provides: "HighCommand singleton detailed design"
      contains: "class HighCommand implements ITaskForceRegistry"
  key_links:
    - from: "Kernel.ts"
      to: "HighCommand.tick()"
      via: "highCommandTick lifecycle stage"
      pattern: "highCommand\\.tick"
    - from: "HighCommand"
      to: "IIntelProvider / IGarrison[] / ITaskForce"
      via: "interface fields (not concrete class types)"
      pattern: "IIntelProvider|IGarrison\\[\\]"
---

<objective>
Write KERN-01: HighCommand 全局单例设计文档.

Purpose: Provide a complete, copy-ready specification of the HighCommand class — its fields, methods, Kernel integration point, and how it implements ITaskForceRegistry — that directly guides coding in a future phase. This is the foundational document; KERN-02/03/04 all reference HighCommand's field structure.

Output: `docs/v2-design/KERN-01-highcommand-singleton.md`
</objective>

<execution_context>
@/Users/lystran/programming/screeps/.claude/get-shit-done/workflows/execute-plan.md
@/Users/lystran/programming/screeps/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/lystran/programming/screeps/.planning/ROADMAP.md
@/Users/lystran/programming/screeps/.planning/phases/09-kernel-layer-design/09-CONTEXT.md
@/Users/lystran/programming/screeps/.planning/phases/09-kernel-layer-design/09-RESEARCH.md
@/Users/lystran/programming/screeps/.planning/phases/09-kernel-layer-design/09-PATTERNS.md

<interfaces>
<!-- Existing source files the executor MUST reference for accuracy. -->

From src/runtime/Kernel.ts (full file — 109 lines):
- Kernel holds `private services: RuntimeServices | null = null`
- Kernel holds `private readonly stages: LifecycleStageOverrides`
- Kernel.run() loops KERNEL_LIFECYCLE_STAGES with try/catch per stage
- Kernel does NOT currently hold a `highCommand` field — design doc must specify adding it
- `shouldProfileStage` skips "refreshServices"
- `createLifecycleContext()` passes `memory`, `game`, `tick`, `services`, `requireServices`

From src/runtime/lifecycle.ts (full file — 39 lines):
- `LifecycleStageName = "refreshServices" | "detectEnvironmentBootstrap"` — Phase 9 adds "highCommandTick"
- `KERNEL_LIFECYCLE_STAGES: LifecycleStage[]` — Phase 9 appends new entry
- `LifecycleStage = { name: LifecycleStageName; run: (context: RuntimeLifecycleContext) => void }`

From src/shared/events/EventBus.ts (full file — 61 lines):
- `EventBus.on<T>(event: string, handler: EventHandler<T>): void`
- `EventBus.emit<T>(event: string, payload: T): void`
- `EventBus.off<T>(event: string, handler: EventHandler<T>): void`
- `EventBus.clear(event?: string): void`

From src/shared/interfaces/index.ts (current state):
- Currently: `export {};` (empty skeleton)
- Phase 9 will add: `export type { ITaskForceRegistry } from "./ITaskForceRegistry"`

From docs/v2-design/SPEC-04-interface-contracts.md (structure reference):
- Header format: `**版本:** v2.0 / **状态:** 生效 / **依赖:** ...`
- Section pattern: `## 1. 总体原则（D-XX）`
- Interface definition: Chinese JSDoc on interface + every method
- Anti-pattern block: `// 禁止 X: ...` with `// ❌` markers and corrections
- Footer: `*文档创建：2026-05-10 | Phase 09-内核层设计*`

From docs/v2-design/SPEC-01-directory-structure.md (style reference):
- Alternative header: `> **状态：** | **日期：** | **关联决策：** | **后续规范:**`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write KERN-01-highcommand-singleton.md</name>
  <files>docs/v2-design/KERN-01-highcommand-singleton.md</files>

  <read_first>
    - src/runtime/Kernel.ts — current Kernel implementation; design doc must describe exactly how to add HighCommand to it
    - src/runtime/lifecycle.ts — current LifecycleStageName union and KERNEL_LIFECYCLE_STAGES array
    - src/shared/events/EventBus.ts — EventBus API; HighCommand holds this instance
    - src/shared/interfaces/index.ts — current empty barrel; doc defines what Phase 9 adds
    - docs/v2-design/SPEC-04-interface-contracts.md — document structure and code block format to replicate exactly
    - .planning/phases/09-kernel-layer-design/09-CONTEXT.md — D-01 through D-06 locked decisions
    - .planning/phases/09-kernel-layer-design/09-PATTERNS.md — KERN-01 section structure and code patterns
    - .planning/phases/09-kernel-layer-design/09-RESEARCH.md — HighCommand field code example + common pitfalls
  </read_first>

  <action>
Create `docs/v2-design/KERN-01-highcommand-singleton.md` following the SPEC-04 `**版本:**` header style (not blockquote style). The document must cover these numbered sections in order:

**Header:**
```
# KERN-01: HighCommand 全局单例设计

**版本:** v2.0
**状态:** 生效
**决策来源:** D-01、D-02、D-03、D-04、D-05、D-06（09-CONTEXT.md）
**关联规范:** SPEC-01（目录结构）、SPEC-03（模块边界）、SPEC-04（接口契约）

---
```

**Section 1: 总体原则（D-04）**
HighCommand 是薄协调者原则。State: "HighCommand.ts 本身只做生命周期调度，所有业务逻辑在 Intel/Garrison 内部。预期实现代码量不超过约 100 行。" Reference D-01 (Kernel thin shell), D-02 (single highCommandTick stage), D-03 (Kernel field holds instance, no global), D-04 (thin coordinator, no business logic), D-05 (Intel/Garrison are independent modules), D-06 (HighCommand self-detects global reset).

**Section 2: 文件位置与目录结构（SPEC-01）**
Show the project structure additions per SPEC-01:
```
src/
├── highCommand/
│   ├── index.ts          # barrel：export { HighCommand }（例外：允许导出具体类）
│   └── HighCommand.ts    # 顶级单例
└── shared/
    ├── interfaces/
    │   ├── index.ts      # 扩展：re-export ITaskForceRegistry, IIntelProvider (minimal)
    │   └── ITaskForceRegistry.ts  # 新增
    └── constants/
        └── cpu.ts        # 新增（CpuBudgetLevel — 见 KERN-02）
```

**Section 3: HighCommand 类定义（D-03、D-04、D-15）**
Provide the complete class definition as a copy-ready TypeScript code block. All fields must have explicit `private`/`public`/`readonly` modifiers and Chinese JSDoc. Field types MUST use interface types (not concrete classes). Use this exact field set:

```typescript
import type { IIntelProvider } from "shared/interfaces";
import type { IGarrison } from "highCommand/garrison";
import type { ITaskForce, ITaskForceRegistry } from "shared/interfaces";
import { EventBus } from "shared/events";
import type { CpuBudgetLevel } from "shared/constants/cpu";
import type { RuntimeServices } from "runtime/services";

/**
 * HighCommand 全局单例：tick 驱动入口，持有所有顶层子系统引用。
 * 只做生命周期协调，不做任何业务决策（D-04）。
 * 由 Kernel 的 highCommandTick 阶段持有和调用（D-03）。
 */
export class HighCommand implements ITaskForceRegistry {
  /** 已知 Garrison 的映射，key 为房间名（D-05） */
  private garrisons: Record<string, IGarrison> = {};

  /** 情报模块引用（接口类型，非 Intel 实现类——SPEC-04 模式 2）*/
  private intel: IIntelProvider | null = null;

  /** 全局 Cache 反向索引，每 tick Refresh 阶段重建（见 KERN-04）*/
  private cache: HighCommandCache = { creepsByTaskForce: {}, creepsByGarrison: {} };

  /** 全局 TaskForce 注册表，build 阶段填充（D-15）*/
  private taskForces: Record<string, ITaskForce> = {};

  /** tick 内同步事件总线（D-16 事件驱动 build）*/
  private readonly eventBus: EventBus;

  /** 事件驱动 build 标志；Run 阶段事件 emit → 下一 tick 触发 build（D-16）*/
  private _needsBuildFlag: boolean = true;

  /** 最后已知的 tick，用于 global reset 检测（D-06）*/
  // NOTE: 存储在 global 对象上，不在 HighCommand 实例字段中
  // global._lastTick: number | undefined

  public constructor() {
    this.eventBus = new EventBus();
  }
}
```

Explain that `global._lastTick` is stored on the Screeps `global` object (not as a class field) because it must survive across ticks but HighCommand instance may be rebuilt on global reset.

**Section 4: tick() 方法——自治生命周期分支（D-06）**
Show the complete `tick(services: RuntimeServices): void` method. Use `tick(services)` parameter style (not constructor injection) per RESEARCH.md Open Question 2 recommendation. Include:
- global reset detection via `global._lastTick !== Game.time - 1`
- `_needsBuildFlag` check combined with reset detection
- build() path: sets `_needsBuildFlag = false`, updates `global._lastTick = Game.time` ONLY on success (pitfall 4 avoidance)
- refresh() path
- `computeBudgetLevel(services)` call to get CpuBudgetLevel
- Critical budget: call `runMinimalSet()` — only refresh + Garrison spawn infrastructure
- Normal/Low/Surplus: call `this.init(services)` then `this.run(services)`

Show the exact code:
```typescript
/**
 * 本 tick 入口：由 Kernel 的 highCommandTick 阶段调用。
 * 自治判断 build vs refresh，不依赖 Kernel 传入决策（D-06）。
 * @param services - Kernel 提供的本 tick 运行时服务（logger、profiler）
 */
public tick(services: RuntimeServices): void {
  const isGlobalReset = (global as Record<string, unknown>)._lastTick !== Game.time - 1;
  const needsBuild = isGlobalReset || this._needsBuildFlag;

  if (needsBuild) {
    try {
      this.build(services);
      this._needsBuildFlag = false;
      (global as Record<string, unknown>)._lastTick = Game.time;
    } catch (error) {
      // Build 失败：保持 _needsBuildFlag = true，下一 tick 重试（D-17）
      Game.notify(`HighCommand build failed at tick ${Game.time}: ${String(error)}`);
      return;
    }
  } else {
    this.refresh(services);
    (global as Record<string, unknown>)._lastTick = Game.time;
  }

  const budgetLevel = this.computeBudgetLevel(services);

  if (budgetLevel === CpuBudgetLevel.Critical) {
    this.runMinimalSet(services);
  } else {
    this.init(services);
    this.run(services);
  }
}
```

**Section 5: Kernel 集成（D-01、D-02、D-03）**
Show how to modify `Kernel.ts` to hold HighCommand and add the new lifecycle stage:

5.1 Kernel 字段修改 — Add to Kernel class:
```typescript
import { HighCommand } from "highCommand";

export class Kernel {
  private readonly stages: LifecycleStageOverrides;
  private services: RuntimeServices | null = null;
  private readonly highCommand: HighCommand;  // D-03: Kernel 字段持有实例

  public constructor(options: KernelOptions = {}) {
    this.stages = options.stages || {};
    this.highCommand = new HighCommand();     // 单例由 Kernel 构造
  }
  // ... 其余方法不变
}
```

5.2 lifecycle.ts 扩展 — Show complete modified file content:
```typescript
// 扩展 LifecycleStageName
export type LifecycleStageName =
  | "refreshServices"
  | "detectEnvironmentBootstrap"
  | "highCommandTick";   // Phase 9 新增（KERN-01/KERN-03）

// KERNEL_LIFECYCLE_STAGES 追加阶段（第三个元素）：
{
  name: "highCommandTick" as const,
  run(context: RuntimeLifecycleContext): void {
    // Kernel 通过 closure 持有 highCommand 实例（D-03）
    // 注意：此处需要 Kernel 重构为将 highCommand 传入，或通过闭包捕获
    // 推荐方案：KERNEL_LIFECYCLE_STAGES 改为工厂函数，接收 highCommand 参数
    context.requireServices(); // 确保 services 已初始化（profiler/logger 可用）
    highCommand.tick(context.requireServices());
  }
}
```

Add a note explaining the factory function pattern: since `KERNEL_LIFECYCLE_STAGES` is currently a static array, Phase 9 implementation should refactor it to `createLifecycleStages(highCommand: HighCommand): LifecycleStage[]` so the `highCommandTick` stage can close over the `highCommand` instance. Show this refactoring approach.

**Section 6: 方法签名摘要**
Table listing all public and private methods of HighCommand with signatures and one-line Chinese descriptions:

| 方法签名 | 可见性 | 说明 |
|---------|--------|------|
| `tick(services: RuntimeServices): void` | public | 本 tick 入口，自治判断 build/refresh |
| `register(ref: string, taskForce: ITaskForce): void` | public | ITaskForceRegistry 实现，Build 阶段注册 TF |
| `unregister(ref: string): void` | public | ITaskForceRegistry 实现，注销 TF |
| `getCreepsByTaskForce(ref: string): string[]` | public | Cache 查询接口（见 KERN-04）|
| `getCreepsByGarrison(garrisonName: string): string[]` | public | Cache 查询接口（见 KERN-04）|
| `build(services: RuntimeServices): void` | private | 完整构造对象树 |
| `refresh(services: RuntimeServices): void` | private | 最小刷新 + Cache 重建 |
| `init(services: RuntimeServices): void` | private | 驱动 Intel/Garrison.init() |
| `run(services: RuntimeServices): void` | private | 驱动 Intel/Garrison.run() |
| `runMinimalSet(services: RuntimeServices): void` | private | 熔断模式：只执行 refresh + spawn |
| `computeBudgetLevel(services: RuntimeServices): CpuBudgetLevel` | private | 计算本 tick CPU 预算等级（见 KERN-02）|
| `rebuildCache(): void` | private | Cache 索引完全重建（见 KERN-04）|

**Section 7: IIntelProvider 最小接口（Phase 9 占位）**
Show the minimal IIntelProvider interface that HighCommand needs for driving init/run. Note that the full interface expands in Phase 11:
```typescript
// src/shared/interfaces/IIntelProvider.ts
/**
 * 情报模块接口：HighCommand 驱动 Intel 生命周期所需的最小契约。
 * Phase 9 只定义 init/run 生命周期方法；完整情报查询接口在 Phase 11 扩展。
 */
export interface IIntelProvider {
  /**
   * 声明阶段：排序 TaskForce、收集 spawn 声明。
   * @param services - 本 tick 运行时服务
   */
  init(services: RuntimeServices): void;

  /**
   * 执行阶段：驱动所有 TaskForce 执行 Creep 行为。
   * @param services - 本 tick 运行时服务
   */
  run(services: RuntimeServices): void;
}
```

**Section 8: 禁止模式（D-03、D-04、D-09）**
Show code blocks with ❌ / ✅ patterns for:
1. 禁止 global 注册 HighCommand (D-03) — `(global as any).highCommand = new HighCommand()` ❌ vs Kernel 字段持有 ✅
2. 禁止 HighCommand 持有具体类引用 (SPEC-04 模式 2) — `private intel: Intel` ❌ vs `private intel: IIntelProvider | null` ✅
3. 禁止 HighCommand 做业务决策 (D-04) — placing `placeOrders()` or `scoreExpansion()` logic in HighCommand ❌
4. 禁止 Run 阶段注册 EventBus 处理器 (SPEC-04 禁止模式 5) — `eventBus.on(...)` inside `run()` ❌ vs inside `build()` ✅
5. 禁止 Build 失败后不重置 needsBuild (D-17 pitfall) — updating `global._lastTick` before Build completes ❌

**Section 9: 关联规范**
- KERN-02: CPU 预算等级 CpuBudgetLevel 数据结构与 computeBudgetLevel() 详细定义
- KERN-03: Build/Refresh/Init/Run 四阶段生命周期详细流程
- KERN-04: Cache 反向索引 rebuildCache() 算法与 ITaskForceRegistry 接口
- SPEC-03: 模块边界规则（HighCommand 依赖方向：只持有接口类型）
- SPEC-04: 接口契约规范（模式 2 共享接口 + 依赖反转）

**Footer:**
```
---

*文档创建：2026-05-10 | Phase 09-内核层设计*
```

**Critical quality checks (apply before saving):**
- Every exported interface and class has Chinese JSDoc
- Every method has `public` or `private` explicitly stated
- No field uses a concrete class type (only interfaces: `IIntelProvider`, `IGarrison`, `ITaskForce`)
- `global._lastTick` is updated ONLY after successful build, not before
- EventBus handler registration shown only in `build()`, never in `run()`
- `IIntelProvider` in `src/shared/interfaces/` (not in `highCommand/intel/`)
  </action>

  <verify>
    <automated>grep -c "class HighCommand implements ITaskForceRegistry" /Users/lystran/programming/screeps/docs/v2-design/KERN-01-highcommand-singleton.md</automated>
  </verify>

  <acceptance_criteria>
    - File exists: `docs/v2-design/KERN-01-highcommand-singleton.md`
    - `grep "class HighCommand implements ITaskForceRegistry" docs/v2-design/KERN-01-highcommand-singleton.md` returns a match
    - `grep "private intel: IIntelProvider" docs/v2-design/KERN-01-highcommand-singleton.md` returns a match (not `private intel: Intel`)
    - `grep "private readonly highCommand: HighCommand" docs/v2-design/KERN-01-highcommand-singleton.md` returns a match (Kernel field)
    - `grep "highCommandTick" docs/v2-design/KERN-01-highcommand-singleton.md` returns a match
    - `grep "global._lastTick" docs/v2-design/KERN-01-highcommand-singleton.md` returns a match
    - `grep "_needsBuildFlag" docs/v2-design/KERN-01-highcommand-singleton.md` returns a match
    - `grep "D-0[1-6]" docs/v2-design/KERN-01-highcommand-singleton.md` returns at least 6 matches
    - Document has 9 numbered top-level sections (## 1. through ## 9.)
    - No section contains `global.highCommand =` as a recommendation (only as a ❌ forbidden pattern)
  </acceptance_criteria>

  <done>KERN-01-highcommand-singleton.md exists in docs/v2-design/ with complete HighCommand class definition (all fields, tick() method, Kernel integration, IIntelProvider minimal interface, method signature table, and anti-pattern section), all TypeScript code blocks are SPEC-02 compliant with explicit access modifiers and Chinese JSDoc, and all D-01 through D-06 decisions are explicitly referenced.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Design doc → Implementation | Design documents are prompts for future coding; incorrect interface types here propagate to implementation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01 | Tampering | Interface field types in KERN-01 | mitigate | Executor must use `IIntelProvider`/`IGarrison` interface types — acceptance_criteria grep for `private intel: IIntelProvider` enforces this |
| T-09-02 | Repudiation | D-01~D-06 decision coverage | mitigate | Acceptance criteria requires `grep "D-0[1-6]"` returns ≥ 6 matches |
| T-09-03 | Elevation of Privilege | global.highCommand registration | mitigate | Anti-pattern section explicitly forbids global registration (D-03); acceptance criteria checks no `global.highCommand =` as recommendation |
</threat_model>

<verification>
After task completes:

```bash
# File exists
ls docs/v2-design/KERN-01-highcommand-singleton.md

# HighCommand implements ITaskForceRegistry
grep "class HighCommand implements ITaskForceRegistry" docs/v2-design/KERN-01-highcommand-singleton.md

# No concrete Intel type in intel field
grep "private intel: IIntelProvider" docs/v2-design/KERN-01-highcommand-singleton.md

# Kernel holds HighCommand
grep "private readonly highCommand: HighCommand" docs/v2-design/KERN-01-highcommand-singleton.md

# All 6 decisions referenced
grep -c "D-0[1-6]" docs/v2-design/KERN-01-highcommand-singleton.md
```
</verification>

<success_criteria>
`docs/v2-design/KERN-01-highcommand-singleton.md` is complete with:
- 9 numbered sections covering all locked decisions D-01 through D-06
- Copy-ready HighCommand class with interface-typed fields (no concrete class references)
- tick() method showing global reset detection and build/refresh branching
- Kernel integration showing how to add `private readonly highCommand` field and `highCommandTick` stage
- IIntelProvider minimal interface (Phase 9 scope, Phase 11 will expand)
- Method signature table for all public + private methods
- Anti-pattern section with ❌/✅ code blocks for 5 forbidden patterns
- SPEC-02 compliant: Chinese JSDoc on all exported symbols, explicit access modifiers, I-prefix interfaces
</success_criteria>

<output>
After completion, create `.planning/phases/09-kernel-layer-design/09-01-SUMMARY.md`
</output>

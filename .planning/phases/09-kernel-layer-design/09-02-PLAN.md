---
phase: 09-kernel-layer-design
plan: 02
type: execute
wave: 2
depends_on:
  - 09-01
files_modified:
  - docs/v2-design/KERN-02-kernel-scheduler.md
autonomous: true
requirements:
  - KERN-02

must_haves:
  truths:
    - "KERN-02-kernel-scheduler.md exists in docs/v2-design/"
    - "Document defines CpuBudgetLevel as const object with Critical/Low/Normal/Surplus values and derived union type"
    - "Document defines ICpuBudgetConfig interface with criticalThreshold/lowThreshold/surplusThreshold and concrete numeric values"
    - "Document defines computeBudgetLevel() method signature (return type CpuBudgetLevel, no implementation body)"
    - "Document defines watchdog data structure (WatchdogRecord) for per-TaskForce monitoring"
    - "Document clearly marks Phase 9 scope boundary: truncation loop and watchdog implementation are Phase 11"
    - "All as const patterns use derived union type (typeof CpuBudgetLevel[keyof typeof CpuBudgetLevel])"
  artifacts:
    - path: "docs/v2-design/KERN-02-kernel-scheduler.md"
      provides: "CPU budget infrastructure design"
      contains: "CpuBudgetLevel"
  key_links:
    - from: "src/shared/constants/cpu.ts"
      to: "CpuBudgetLevel"
      via: "as const object + derived union type"
      pattern: "as const"
    - from: "HighCommand.computeBudgetLevel"
      to: "ICpuBudgetConfig"
      via: "threshold comparison against Game.cpu.bucket"
      pattern: "computeBudgetLevel"
---

<objective>
Write KERN-02: Kernel 调度器与 CPU 预算基础设施设计文档.

Purpose: Define the CPU budget infrastructure (CpuBudgetLevel enum, ICpuBudgetConfig threshold interface, computeBudgetLevel() method signature, watchdog data structure) that HighCommand uses in tick() to decide execution scope. Phase 9 scope is data structures and interfaces only — truncation execution loop and watchdog suspend logic are Phase 11 scope (D-11).

Output: `docs/v2-design/KERN-02-kernel-scheduler.md`
</objective>

<execution_context>
@/Users/lystran/programming/screeps/.claude/get-shit-done/workflows/execute-plan.md
@/Users/lystran/programming/screeps/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/lystran/programming/screeps/.planning/ROADMAP.md
@/Users/lystran/programming/screeps/.planning/phases/09-kernel-layer-design/09-CONTEXT.md
@/Users/lystran/programming/screeps/.planning/phases/09-kernel-layer-design/09-RESEARCH.md
@/Users/lystran/programming/screeps/.planning/phases/09-kernel-layer-design/09-PATTERNS.md
@/Users/lystran/programming/screeps/.planning/phases/09-kernel-layer-design/09-01-SUMMARY.md

<interfaces>
<!-- Key types from KERN-01 that KERN-02 must be consistent with -->

From KERN-01 design (09-01-SUMMARY.md or KERN-01 doc):
- HighCommand has `private computeBudgetLevel(services: RuntimeServices): CpuBudgetLevel`
- CpuBudgetLevel is imported from "shared/constants/cpu"
- HighCommand.tick() branches on CpuBudgetLevel.Critical to call runMinimalSet()

From docs/v2-design/SPEC-02-code-style.md (as const pattern):
```typescript
export const TaskForceType = {
  Mining: "mining",
  Logistics: "logistics",
} as const;
export type TaskForceType = typeof TaskForceType[keyof typeof TaskForceType];
```

From docs/v2-design/SPEC-01-directory-structure.md (KERN-02 header style uses > blockquote):
```markdown
> **状态：** v2.0 权威文档 | **日期：** 2026-05-10
> **关联决策：** D-07、D-08、D-09、D-10、D-11（09-CONTEXT.md）
```

From src/profiling/Profiler.ts — startStage/endStage interface (don't hand-roll CPU measurement).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write KERN-02-kernel-scheduler.md</name>
  <files>docs/v2-design/KERN-02-kernel-scheduler.md</files>

  <read_first>
    - .planning/phases/09-kernel-layer-design/09-CONTEXT.md — D-07 through D-11 locked decisions (exact wording)
    - .planning/phases/09-kernel-layer-design/09-RESEARCH.md — Pattern 5 (CPU budget data structure), Pitfall 3 (Phase 9 scope boundary), Common Pitfalls section
    - .planning/phases/09-kernel-layer-design/09-PATTERNS.md — KERN-02 section structure and as const pattern
    - docs/v2-design/SPEC-02-code-style.md — as const + derived union type pattern (lines 159-175)
    - docs/v2-design/SPEC-01-directory-structure.md — blockquote header style to use for KERN-02
    - docs/v2-design/KERN-01-highcommand-singleton.md — computeBudgetLevel() signature already established in KERN-01
    - src/profiling/Profiler.ts — startStage/endStage interface for watchdog design reference
    - .planning/phases/09-kernel-layer-design/09-01-SUMMARY.md — KERN-01 decisions that KERN-02 builds on
  </read_first>

  <action>
Create `docs/v2-design/KERN-02-kernel-scheduler.md` using the SPEC-01 blockquote header style. The document covers 7 numbered sections:

**Header:**
```
# KERN-02: Kernel 调度器与 CPU 预算基础设施

> **状态：** v2.0 权威文档 | **日期：** 2026-05-10
> **关联决策：** D-07、D-08、D-09、D-10、D-11（09-CONTEXT.md）
> **阶段范围：** Phase 9 只定义数据结构与接口；截断执行和看门狗实现在 Phase 11

---
```

**Section 1: 总体原则（D-07、D-11）**
Explain the truncation model (D-07): execute TaskForces in priority order, truncate when approaching CPU limit. NOT pre-allocated budgets per subsystem. Explain Phase 9 scope boundary explicitly (D-11): this document only covers (1) CpuBudgetLevel data structure, (2) ICpuBudgetConfig threshold interface, (3) computeBudgetLevel() method signature. Truncation for-loop and watchdog suspend counter implementation are Phase 11.

**Section 2: CpuBudgetLevel 预算等级（D-08）**
Show complete `src/shared/constants/cpu.ts` as a copy-ready code block. Use Screeps bucket range 0–10000. Claude's Discretion for threshold values — use these specific values:
- Critical: bucket < 500 (less than 5% capacity — creep safety only)
- Low: bucket < 2500 (less than 25% — truncate low-priority TaskForces)
- Normal: bucket 2500–8000 — full execution
- Surplus: bucket > 8000 (80%+ — enable optional intensive tasks)

```typescript
// src/shared/constants/cpu.ts

/**
 * CPU Bucket 水位等级常量。Screeps bucket 范围 0–10000。
 * 等级决定本 tick 可执行的服务范围；截断策略在 Phase 11 Intel 调度器中实现（D-11）。
 */
export const CpuBudgetLevel = {
  /** Bucket < 500：极低水位，只执行最小集（refresh + spawn 基础设施）（D-10）*/
  Critical: "critical",
  /** Bucket < 2500：偏低水位，截断低优先级 TaskForce（D-07）*/
  Low: "low",
  /** Bucket 2500–8000：正常水位，完整执行所有 TaskForce */
  Normal: "normal",
  /** Bucket > 8000：充足水位，可执行可选的性能密集任务 */
  Surplus: "surplus"
} as const;

/**
 * 从常量对象派生联合类型（SPEC-02 D-13 规范）。
 */
export type CpuBudgetLevel = typeof CpuBudgetLevel[keyof typeof CpuBudgetLevel];
```

**Section 3: ICpuBudgetConfig 阈值接口（D-08）**
Show complete `src/shared/interfaces/ICpuBudgetConfig.ts`:

```typescript
// src/shared/interfaces/ICpuBudgetConfig.ts

/**
 * CPU 预算阈值配置接口。
 * 运行时通过此接口读取各水位临界值，具体数值在 HighCommand 构造时初始化。
 */
export interface ICpuBudgetConfig {
  /** Bucket 低于此值时进入 Critical 模式（推荐值：500）*/
  readonly criticalThreshold: number;
  /** Bucket 低于此值时进入 Low 模式（推荐值：2500）*/
  readonly lowThreshold: number;
  /** Bucket 高于此值时进入 Surplus 模式（推荐值：8000）*/
  readonly surplusThreshold: number;
}
```

Then show the default config constant:
```typescript
// src/shared/constants/cpu.ts（追加到 CpuBudgetLevel 之后）
import type { ICpuBudgetConfig } from "shared/interfaces";

/**
 * 默认 CPU 预算阈值配置（基于 Screeps bucket 上限 10000）。
 * Phase 11 可通过注入自定义 ICpuBudgetConfig 覆盖此配置。
 */
export const DEFAULT_CPU_BUDGET_CONFIG: ICpuBudgetConfig = {
  criticalThreshold: 500,
  lowThreshold: 2500,
  surplusThreshold: 8000
} as const;
```

**Section 4: computeBudgetLevel() 方法签名（D-08）**
Show the method signature as it appears in HighCommand (from KERN-01). No implementation body — only the signature and behavior contract:

```typescript
/**
 * 计算本 tick 的 CPU 预算等级，基于当前 bucket 水位。
 * 只读取 Game.cpu.bucket，不产生副作用。
 * @param services - 本 tick 运行时服务（用于 logger 记录异常水位）
 * @returns 本 tick 适用的 CpuBudgetLevel
 */
private computeBudgetLevel(services: RuntimeServices): CpuBudgetLevel {
  // 实现：对照 ICpuBudgetConfig 阈值比较 Game.cpu.bucket
  // 返回 CpuBudgetLevel.Critical / Low / Normal / Surplus
  // 注意：bucket 是 Game.cpu.bucket（0-10000），不是 Game.cpu.getUsed()
  throw new Error("computeBudgetLevel implementation in Phase 11");
}
```

Add a note: Phase 9 establishes the method contract. The full implementation comparing `Game.cpu.bucket` against `ICpuBudgetConfig` thresholds is written during Phase 11.

Show the bucket level → service scope decision table:

| CpuBudgetLevel | Game.cpu.bucket | 可执行服务范围 |
|----------------|-----------------|----------------|
| Surplus | > 8000 | 完整执行 + 可选密集任务 |
| Normal | 2500–8000 | 完整执行所有 TaskForce |
| Low | 500–2500 | 截断低优先级 TaskForce（Phase 11 实现）|
| Critical | < 500 | 最小集：refresh + spawn 基础设施（D-10）|

**Section 5: 看门狗数据结构（D-09）**
Define the watchdog per-TaskForce monitoring data structure. Phase 9 defines ONLY the data structure — the suspend logic and consecutive-tick counting is Phase 11.

```typescript
// src/highCommand/types.ts（HighCommand 域私有类型，非 shared 接口）

/**
 * 看门狗监控记录：追踪单个 TaskForce 的 CPU 超限历史。
 * 看门狗策略在 Phase 11 Intel 调度器中实现（D-09、D-11）。
 */
export interface WatchdogRecord {
  /** TaskForce 的唯一标识符 */
  readonly ref: string;
  /** 连续超限 tick 计数（超过阈值后触发 suspend）*/
  consecutiveOverrunTicks: number;
  /** 是否当前被 suspend（suspend 时跳过 init/run）*/
  suspended: boolean;
  /** suspend 解除的 tick 时间（Game.time 值）*/
  suspendUntilTick: number;
}

/**
 * 看门狗配置接口（Phase 9 定义接口，阈值数值在 Phase 11 设置）。
 */
export interface IWatchdogConfig {
  /** 单个 TaskForce 单 tick CPU 警告阈值（毫秒，推荐值：5）*/
  readonly perTickWarningMs: number;
  /** 连续超限 tick 数达到此值时触发 suspend（推荐值：3）*/
  readonly consecutiveOverrunLimit: number;
  /** suspend 持续 tick 数（推荐值：10）*/
  readonly suspendDurationTicks: number;
}
```

**Section 6: 截断与熔断边界说明（D-10、D-11）**
Explicitly state what is OUT OF SCOPE for Phase 9 and delegated to Phase 11. Use a table:

| 能力 | Phase 9（本文档定义） | Phase 11（Intel 调度器实现）|
|------|---------------------|--------------------------|
| CpuBudgetLevel 数据结构 | ✅ CpuBudgetLevel as const | — |
| ICpuBudgetConfig 阈值接口 | ✅ 接口定义 + 推荐值 | — |
| computeBudgetLevel() 方法签名 | ✅ 方法签名 + 行为契约 | 实现体 |
| WatchdogRecord 数据结构 | ✅ 接口定义 | — |
| TaskForce 截断 for 循环 | — | ✅ Intel 调度器按优先级截断 |
| 看门狗 suspend/resume 逻辑 | — | ✅ 连续超限计数 + suspend |
| 熔断最小集执行 | HighCommand.runMinimalSet() 签名 | runMinimalSet() 实现体 |

Include the explicit quote from D-11: "Phase 9 只定义 CPU 管理的基础设施（预算等级数据结构、阈值接口、HighCommand 计算本 tick 预算等级的逻辑）。截断执行和看门狗的具体实现留给 Phase 11。"

**Section 7: 关联规范**
- KERN-01: computeBudgetLevel() 在 HighCommand.tick() 中的调用位置
- KERN-03: Init/Run 阶段的执行条件（budgetLevel !== Critical）
- SPEC-02: as const 对象 + 派生联合类型模式（D-13）
- SPEC-04: shared/ 只读接口模式（模式 5）— CpuBudgetLevel 和 ICpuBudgetConfig 均为 shared 层内容

**Footer:**
```
---

*文档创建：2026-05-10 | Phase 09-内核层设计*
```

**Critical quality checks:**
- CpuBudgetLevel as const object uses string values ("critical" not 0/1/2/3)
- Derived union type uses `typeof CpuBudgetLevel[keyof typeof CpuBudgetLevel]`
- ICpuBudgetConfig fields are `readonly`
- WatchdogRecord is in `highCommand/types.ts` (domain-private), NOT in `shared/interfaces/`
- No for-loop truncation code appears in this document (Phase 11 scope)
- Section 6 boundary table clearly separates Phase 9 vs Phase 11 deliverables
  </action>

  <verify>
    <automated>grep -c "CpuBudgetLevel" /Users/lystran/programming/screeps/docs/v2-design/KERN-02-kernel-scheduler.md</automated>
  </verify>

  <acceptance_criteria>
    - File exists: `docs/v2-design/KERN-02-kernel-scheduler.md`
    - `grep "as const" docs/v2-design/KERN-02-kernel-scheduler.md | grep -c "CpuBudgetLevel"` returns ≥ 1
    - `grep "ICpuBudgetConfig" docs/v2-design/KERN-02-kernel-scheduler.md` returns a match
    - `grep "computeBudgetLevel" docs/v2-design/KERN-02-kernel-scheduler.md` returns a match
    - `grep "WatchdogRecord" docs/v2-design/KERN-02-kernel-scheduler.md` returns a match
    - `grep "Phase 11" docs/v2-design/KERN-02-kernel-scheduler.md | wc -l` returns ≥ 3 (scope boundary clearly marked)
    - `grep "D-0[7-9]\|D-10\|D-11" docs/v2-design/KERN-02-kernel-scheduler.md` returns ≥ 5 matches
    - `grep "typeof CpuBudgetLevel\[keyof typeof CpuBudgetLevel\]" docs/v2-design/KERN-02-kernel-scheduler.md` returns a match
    - Document has 7 numbered top-level sections (## 1. through ## 7.)
    - `grep "for.*if.*Game.cpu.getUsed" docs/v2-design/KERN-02-kernel-scheduler.md` returns no matches (truncation loop must not appear)
  </acceptance_criteria>

  <done>KERN-02-kernel-scheduler.md exists in docs/v2-design/ with CpuBudgetLevel as const object (Critical/Low/Normal/Surplus with concrete threshold values), ICpuBudgetConfig interface, computeBudgetLevel() method signature, WatchdogRecord data structure, and a clear Phase 9 vs Phase 11 scope boundary table — no truncation loop code present.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Design doc → Phase 11 | Scope boundary must be unambiguous; Phase 11 implementor must know exactly what KERN-02 defines vs defers |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-04 | Tampering | Phase 9/11 scope boundary in KERN-02 | mitigate | Acceptance criteria checks `grep "Phase 11" ... | wc -l` ≥ 3 and no for-loop truncation code |
| T-09-05 | Information Disclosure | WatchdogRecord placement | mitigate | WatchdogRecord must be in `highCommand/types.ts` (domain-private), not exported from `shared/interfaces/` |
</threat_model>

<verification>
After task completes:

```bash
# File exists
ls docs/v2-design/KERN-02-kernel-scheduler.md

# CpuBudgetLevel as const present
grep "as const" docs/v2-design/KERN-02-kernel-scheduler.md

# Derived union type present
grep "typeof CpuBudgetLevel" docs/v2-design/KERN-02-kernel-scheduler.md

# Phase 11 scope boundary mentioned multiple times
grep "Phase 11" docs/v2-design/KERN-02-kernel-scheduler.md | wc -l

# No truncation for-loop code
grep "Game.cpu.getUsed" docs/v2-design/KERN-02-kernel-scheduler.md
```
</verification>

<success_criteria>
`docs/v2-design/KERN-02-kernel-scheduler.md` is complete with:
- CpuBudgetLevel as const object with string values (not numbers) and derived union type
- ICpuBudgetConfig interface with concrete threshold recommendations (500/2500/8000)
- DEFAULT_CPU_BUDGET_CONFIG constant implementing ICpuBudgetConfig
- computeBudgetLevel() method signature with behavior contract (no implementation body)
- WatchdogRecord and IWatchdogConfig data structures (domain-private types)
- Phase 9 vs Phase 11 scope boundary table with explicit D-11 quote
- SPEC-02 compliant: as const + derived type pattern, Chinese JSDoc on all exports
</success_criteria>

<output>
After completion, create `.planning/phases/09-kernel-layer-design/09-02-SUMMARY.md`
</output>

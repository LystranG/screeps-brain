---
phase: 09-kernel-layer-design
plan: 03
type: execute
wave: 2
depends_on:
  - 09-01
files_modified:
  - docs/v2-design/KERN-03-lifecycle.md
autonomous: true
requirements:
  - KERN-03

must_haves:
  truths:
    - "KERN-03-lifecycle.md exists in docs/v2-design/"
    - "Document shows exact LifecycleStageName union extension adding 'highCommandTick' as third member"
    - "Document shows KERNEL_LIFECYCLE_STAGES array with third entry for highCommandTick"
    - "Document covers all four phases (Build/Refresh/Init/Run) with precise trigger conditions and execution sequence"
    - "Document covers D-16 through D-19 locked decisions with explicit references"
    - "Build failure recovery is explicitly specified: _needsBuildFlag stays true, global._lastTick not updated on failure"
    - "EventBus handler registration shown in build(), emit shown in run() — not reversed"
    - "HighCommandEvents as const object defines event name constants"
  artifacts:
    - path: "docs/v2-design/KERN-03-lifecycle.md"
      provides: "Four-phase lifecycle execution flow and boundary conditions"
      contains: "highCommandTick"
  key_links:
    - from: "src/runtime/lifecycle.ts"
      to: "highCommandTick stage"
      via: "LifecycleStageName union + KERNEL_LIFECYCLE_STAGES array"
      pattern: "highCommandTick"
    - from: "HighCommand.build()"
      to: "EventBus.on(HighCommandEvents.StructureBuilt)"
      via: "Build-phase handler registration"
      pattern: "eventBus\\.on"
---

<objective>
Write KERN-03: Build/Refresh/Init/Run 四阶段生命周期设计文档.

Purpose: Define the precise execution flow, trigger conditions, and boundary conditions for each of the four lifecycle phases in HighCommand. This document is the authoritative reference for when each phase runs, what it does, how exceptions are recovered, and how EventBus enables event-driven build triggers. It also specifies the exact changes to `lifecycle.ts` and `Kernel.ts` needed to integrate HighCommand.

Output: `docs/v2-design/KERN-03-lifecycle.md`
</objective>

<execution_context>
@/Users/lystran/programming/screeps/.claude/get-shit-done/workflows/execute-plan.md
@/Users/lystran/programming/screeps/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/lystran/programming/screeps/.planning/ROADMAP.md
@/Users/lystran/programming/screeps/.planning/phases/09-kernel-layer-design/09-CONTEXT.md
@/Users/lystran/programming/screeps/.planning/phases/09-kernel-layer-design/09-RESEARCH.md
@/Users/lystran/programming/screeps/.planning/phases/09-kernel-layer-design/09-PATTERNS.md
@/Users/lystran/programming/screeps/.planning/phases/09-kernel-layer-design/09-01-SUMMARY.md

<interfaces>
<!-- Exact current state of lifecycle.ts and Kernel.ts that the design doc must reference and show modifications to -->

From src/runtime/lifecycle.ts (current — 39 lines):
```typescript
export type LifecycleStageName =
  | "refreshServices"
  | "detectEnvironmentBootstrap";

export const KERNEL_LIFECYCLE_STAGES: LifecycleStage[] = [
  { name: "refreshServices", run(context) { context.services = createRuntimeServices(context.memory, context.game); } },
  { name: "detectEnvironmentBootstrap", run: runEnvironmentBootstrapStage }
];
export const KERNEL_STAGE_ORDER: LifecycleStageName[] = KERNEL_LIFECYCLE_STAGES.map(stage => stage.name);
```

From src/runtime/Kernel.ts (key parts):
- `Kernel.run()` loops `createLifecycleStages()` with try/catch per stage
- `shouldProfileStage()` skips "refreshServices"
- `createLifecycleContext()` passes `requireServices: () => this.requireServices()`
- Kernel currently does NOT hold a HighCommand field

From src/shared/events/EventBus.ts:
- `on<T>(event, handler): void` — register (Build phase)
- `emit<T>(event, payload): void` — dispatch (Run phase)
- `off<T>(event, handler): void` — deregister
- `clear(event?: string): void` — clear all handlers

From docs/v2-design/SPEC-04-interface-contracts.md (EventBus constraint block pattern):
```markdown
**重要约束：**
- 纯同步，无 `async`/`Promise`/`setTimeout`
- 处理器必须在 Build 阶段注册，不能在 Run 阶段注册（见禁止模式 5）
- 避免循环事件（事件 A 触发的处理器中不能再 emit 事件 A）
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write KERN-03-lifecycle.md</name>
  <files>docs/v2-design/KERN-03-lifecycle.md</files>

  <read_first>
    - src/runtime/lifecycle.ts — exact current content; design doc shows before/after for LifecycleStageName and KERNEL_LIFECYCLE_STAGES
    - src/runtime/Kernel.ts — current Kernel implementation; shows how the factory function refactor enables HighCommand integration
    - src/shared/events/EventBus.ts — on/emit/off/clear API exact signatures
    - .planning/phases/09-kernel-layer-design/09-CONTEXT.md — D-16 through D-19 locked decisions (exact wording)
    - .planning/phases/09-kernel-layer-design/09-RESEARCH.md — Pattern 1 (stage extension), Pattern 2 (tick() method), Pitfall 4 (Build failure state), Pitfall 5 (Run-phase handler registration), lifecycle diagram
    - .planning/phases/09-kernel-layer-design/09-PATTERNS.md — KERN-03 section structure with 10 numbered sections
    - docs/v2-design/SPEC-04-interface-contracts.md — EventBus usage pattern and constraint block format
    - docs/v2-design/KERN-01-highcommand-singleton.md — tick() method already defined in KERN-01; KERN-03 expands on each phase
    - .planning/phases/09-kernel-layer-design/09-01-SUMMARY.md
  </read_first>

  <action>
Create `docs/v2-design/KERN-03-lifecycle.md` using the SPEC-01 blockquote header style. The document covers 10 numbered sections:

**Header:**
```
# KERN-03: Build/Refresh/Init/Run 四阶段生命周期

> **状态：** v2.0 权威文档 | **日期：** 2026-05-10
> **关联决策：** D-16、D-17、D-18、D-19（09-CONTEXT.md）
> **依赖：** KERN-01（HighCommand 单例字段结构）、SPEC-04（EventBus 通信模式 4）

---
```

**Section 1: 总体原则（D-18）**
"对象引用即状态": Build 阶段构造对象树，Refresh 阶段更新引用，Init/Run 通过对象引用访问数据，不需要中间结果传递机制。The architecture diagram from RESEARCH.md (the ASCII flowchart showing: loop → Kernel.run() → highCommandTick → HighCommand.tick() → build/refresh → init → run). Include the full ASCII diagram from 09-RESEARCH.md §Architecture Patterns §System Architecture Diagram. Then state: "Global 对象树生命周期：Build 时创建，Refresh 时更新，下次 Build 时整体替换。没有部分重建逻辑。"

**Section 2: lifecycle.ts 扩展（KERN-03）**
Show the exact before/after comparison.

Before (current v2.0 skeleton):
```typescript
// src/runtime/lifecycle.ts — 当前骨架
export type LifecycleStageName =
  | "refreshServices"
  | "detectEnvironmentBootstrap";

export const KERNEL_LIFECYCLE_STAGES: LifecycleStage[] = [
  { name: "refreshServices", run(context) { ... } },
  { name: "detectEnvironmentBootstrap", run: runEnvironmentBootstrapStage }
];
```

After (Phase 9 扩展):
```typescript
// src/runtime/lifecycle.ts — Phase 9 扩展后
import { HighCommand } from "highCommand";  // 注意：lifecycle.ts 需要 import HighCommand

export type LifecycleStageName =
  | "refreshServices"
  | "detectEnvironmentBootstrap"
  | "highCommandTick";   // Phase 9 新增（KERN-03）

// 重构为工厂函数：lifecycle.ts 接收 highCommand 实例参数
// 原因：KERNEL_LIFECYCLE_STAGES 是静态数组，无法捕获 Kernel 实例字段
export function createLifecycleStages(highCommand: HighCommand): LifecycleStage[] {
  return [
    {
      name: "refreshServices",
      run(context: RuntimeLifecycleContext): void {
        context.services = createRuntimeServices(context.memory, context.game);
      }
    },
    {
      name: "detectEnvironmentBootstrap",
      run: runEnvironmentBootstrapStage
    },
    {
      name: "highCommandTick",
      run(context: RuntimeLifecycleContext): void {
        // Kernel 已确保前序阶段成功（refreshServices + detectEnvironmentBootstrap）
        // requireServices() 在此处安全可用
        highCommand.tick(context.requireServices());
      }
    }
  ];
}

// 保持向后兼容的 KERNEL_STAGE_ORDER（从 LifecycleStageName 类型派生，无需改动）
```

Add a note: This refactoring changes the export from `KERNEL_LIFECYCLE_STAGES` (array constant) to `createLifecycleStages()` (factory function). Kernel.ts must be updated to call `createLifecycleStages(this.highCommand)` instead of referencing the static array. The `KERNEL_STAGE_ORDER` derivation remains valid from the type.

Show the corresponding Kernel.ts modification (the `createLifecycleStages` call site):
```typescript
// src/runtime/Kernel.ts — 修改后的 createLifecycleStages 调用
private createLifecycleStages(): LifecycleStage[] {
  // 修改前：return KERNEL_LIFECYCLE_STAGES.map(...)
  // 修改后：调用工厂函数，传入 this.highCommand
  return createLifecycleStages(this.highCommand).map(stage => ({
    name: stage.name,
    run: this.stages[stage.name] || stage.run
  }));
}
```

**Section 3: Build 阶段（D-16）**
3.1 触发条件（D-16）— Three triggers, listed precisely:
1. global reset: `(global as Record<string, unknown>)._lastTick !== Game.time - 1`（含首次运行，此时 `_lastTick` 为 `undefined`）
2. 手动命令：Console 调用 `highCommand.forceBuild()` 设置 `_needsBuildFlag = true`
3. 事件驱动：Run 阶段 emit `HighCommandEvents.StructureBuilt` 或 `HighCommandEvents.RoomClaimed` → Build 阶段注册的处理器设置 `_needsBuildFlag = true` → 下一 tick 触发 build

3.2 Build 执行内容，ordered list:
1. `this.eventBus.clear()` — 清除上次 Build 的所有 EventBus 处理器
2. 构造 Intel 实例（`new Intel(this.eventBus)`）并赋值 `this.intel`
3. 遍历 Memory.rooms（owned rooms），构造 Garrison 实例，填充 `this.garrisons`
4. 构造每个 Garrison 内的 Facility 树（Barracks、HQ、Watchtower 等）
5. 构造所有 TaskForce 实例（注入 `ITaskForceRegistry = this`）
6. 注册结构变化 EventBus 处理器（`this.eventBus.on(HighCommandEvents.StructureBuilt, ...)`)
7. 调用 `this.rebuildCache()`（完整 Cache 重建）
8. 只有全部完成后才更新：`this._needsBuildFlag = false` 和 `(global as Record<string, unknown>)._lastTick = Game.time`

3.3 异常恢复（D-17）— 包含 try/catch 伪代码：

```typescript
private build(services: RuntimeServices): void {
  // 重建对象树（全部在 try 块内）
  try {
    this.eventBus.clear();           // 清除上次处理器，避免重复注册
    this.intel = new Intel(this.eventBus);
    // ... 构造 Garrison 树 ...
    this.rebuildCache();
    // ✅ 成功：更新标志（在 tick() 中由调用方执行，不在 build() 内部）
  } catch (error) {
    // Build 失败：不更新 _lastTick / _needsBuildFlag（由 tick() 保持 true）
    Game.notify(`HighCommand.build() 失败 at tick ${Game.time}: ${String(error)}`);
    throw error; // 重新抛出，让 tick() 处理失败路径
  }
}
```

Add constraint block:
```
**重要约束：Build 阶段**
- global._lastTick 只在 build() 成功后更新（由 tick() 在 catch 之外执行）
- _needsBuildFlag 在 build() 失败时保持 true（下一 tick 重试 build）
- eventBus.clear() 必须是 build() 的第一步（防止处理器重复注册）
- Build 失败通过 Game.notify() 发送邮件通知（D-17）
```

**Section 4: Refresh 阶段（D-19）**
Explain minimal responsibility: only id→object reference refresh + cache rebuild. NOT structure change detection.

```typescript
private refresh(services: RuntimeServices): void {
  // 1. 刷新 id → Game 对象引用（O(结构数量) 的 getObjectById 调用）
  for (const garrison of Object.values(this.garrisons)) {
    garrison.refresh();   // 每个 Garrison 刷新其 Facility 引用
  }
  if (this.intel !== null) {
    this.intel.refresh(); // Intel 刷新其内部引用（Phase 11 详细设计）
  }

  // 2. 完整重建 Cache 索引（D-12）
  this.rebuildCache();

  // 3. 检测 _needsBuildFlag（由 Run 阶段事件设置）
  // 注意：needsBuild 检测在 tick() 入口，不在 refresh() 内部
  // refresh() 只做刷新，不做决策
}
```

Add constraint block:
```
**重要约束：Refresh 阶段（D-19）**
- Refresh 不检测结构变化（新 Storage、新 claim 房间）
- 结构变化通过 Run 阶段 emit 事件 → 设置 _needsBuildFlag → 下一 tick build
- Refresh 不调用 Game.notify()（只有 build 失败才发通知）
```

**Section 5: Init 阶段**
Execution sequence (ordered):
1. `this.intel?.init(services)` → Intel 排序 TaskForce 优先级，收集 spawn wishlist
2. `for (const garrison of Object.values(this.garrisons)) { garrison.init(services); }` → 每个 Garrison 调用 Facility.init()

Show that Init produces NO game side effects — only declarations (spawn requests enqueued to Barracks, resource requests declared). Barracks.handleSpawns() is called in Run, not Init.

Add constraint block:
```
**重要约束：Init 阶段**
- Init 阶段不产生任何 Game 副作用（不 spawn、不移动 Creep、不转移资源）
- Init 阶段不调用 eventBus.emit()（事件分发在 Run 阶段）
- Init 阶段异常：单模块 try/catch 隔离，不影响其他模块（D-17）
```

**Section 6: Run 阶段**
Execution sequence (ordered):
1. `this.intel?.run(services)` → Intel 执行 TaskForce.run() (Creep 行为)
2. `for (const garrison of Object.values(this.garrisons)) { garrison.run(services); }` → 每个 Garrison 执行 Facility.run()
   - 包含 `Barracks.handleSpawns()` — 按优先级执行 spawn 请求
   - 包含 `HQ.run()` — 执行资源调配
   - 等等

Show that Run is where game mutations happen (creep.moveTo, spawn.spawnCreep, structure.transfer, etc.) and where EventBus emit calls happen.

Add constraint block:
```
**重要约束：Run 阶段**
- Run 阶段不调用 eventBus.on()（处理器注册在 Build 阶段）
- Run 阶段可以调用 eventBus.emit()（分发事件给 Build 阶段注册的处理器）
- Run 阶段异常：单模块 try/catch 隔离，不影响其他模块（D-17）
```

**Section 7: 异常恢复策略（D-17）**
Table showing the two-level recovery strategy:

| 失败场景 | 恢复策略 | 下一 tick 行为 |
|---------|---------|----------------|
| Build 失败（异常抛出）| `_needsBuildFlag` 保持 true，`global._lastTick` 不更新，Game.notify() | 下一 tick 重新执行 build |
| Init/Run 单模块失败 | 单模块 try/catch 隔离，其他模块继续执行，Game.notify() | 下一 tick 正常 refresh + init/run（失败模块继续尝试）|
| Init/Run 多模块连续失败 | 同上（各自隔离），不触发 build | 同上 |

Show the isolated try/catch pattern for Init/Run:
```typescript
private init(services: RuntimeServices): void {
  // Intel init（隔离）
  try {
    this.intel?.init(services);
  } catch (error) {
    Game.notify(`Intel.init() 失败 at tick ${Game.time}: ${String(error)}`);
  }

  // Garrison init（各自隔离）
  for (const [roomName, garrison] of Object.entries(this.garrisons)) {
    try {
      garrison.init(services);
    } catch (error) {
      Game.notify(`Garrison[${roomName}].init() 失败 at tick ${Game.time}: ${String(error)}`);
    }
  }
}
```

**Section 8: 事件驱动 Build 通知（D-16）**
8.1 HighCommandEvents 常量（`as const` pattern per SPEC-02 D-13）:
```typescript
// src/shared/events/HighCommandEvents.ts（或 src/shared/constants/events.ts）

/**
 * HighCommand 事件名称常量：定义触发下一 tick build 的结构变化事件。
 * 使用 as const 对象（SPEC-02 D-13），禁止散落的字符串字面量。
 */
export const HighCommandEvents = {
  /** 新 Storage 或 Terminal 建造完成，触发 Garrison 对象树重建 */
  StructureBuilt: "highCommand.structureBuilt",
  /** 新房间被 claim，触发新 Garrison 创建 */
  RoomClaimed: "highCommand.roomClaimed",
  /** 房间失去控制权，触发 Garrison 销毁 */
  RoomLost: "highCommand.roomLost"
} as const;

/**
 * 从常量对象派生联合类型。
 */
export type HighCommandEvent = typeof HighCommandEvents[keyof typeof HighCommandEvents];
```

8.2 处理器注册（Build 阶段）:
```typescript
// 在 HighCommand.build() 中注册处理器（每次 build 前 eventBus.clear() 已清除旧处理器）
private build(services: RuntimeServices): void {
  this.eventBus.clear(); // ← 清除上次处理器

  // 注册结构变化事件处理器
  this.eventBus.on(HighCommandEvents.StructureBuilt, () => {
    this._needsBuildFlag = true; // 下一 tick 触发 build
  });
  this.eventBus.on(HighCommandEvents.RoomClaimed, () => {
    this._needsBuildFlag = true;
  });
  this.eventBus.on(HighCommandEvents.RoomLost, () => {
    this._needsBuildFlag = true;
  });

  // ... 构造对象树 ...
}
```

8.3 事件发出（Run 阶段）— show call site example in a Facility:
```typescript
// 在 HQ.run() 内（检测到 Storage 建造完成时）：
if (this.room.storage !== null && this._lastKnownStorageId === null) {
  this.eventBus.emit(HighCommandEvents.StructureBuilt, { roomName: this.room.name });
}
```

**Section 9: 禁止模式**
Five forbidden patterns with ❌/✅ code blocks:
1. Kernel 参与 build/refresh 决策 (D-02/D-06) — reading `global._lastTick` in Kernel ❌
2. Run 阶段注册 EventBus 处理器 (SPEC-04 禁止模式 5) — `eventBus.on(...)` in `run()` ❌ vs `build()` ✅
3. Build 失败后更新 `global._lastTick` (D-17 pitfall 4) — updating `_lastTick` before `build()` completes ❌
4. Refresh 检测结构变化 (D-19) — `if (this.room.storage !== null && ...)` in `refresh()` ❌ vs emit in `run()` ✅
5. Init 阶段产生 Game 副作用 — calling `creep.moveTo()` in Init ❌

**Section 10: 关联规范**
- KERN-01: HighCommand 单例字段结构，tick() 完整方法定义
- KERN-02: computeBudgetLevel() 与 CpuBudgetLevel（Init/Run 的执行条件）
- KERN-04: rebuildCache() 算法（Refresh 阶段调用）
- SPEC-04: EventBus 通信模式 4（Build 注册/Run 分发规则）
- SPEC-03: 依赖方向（lifecycle.ts 可以 import HighCommand，因为 Kernel 层包含 highCommand 实例）

**Footer:**
```
---

*文档创建：2026-05-10 | Phase 09-内核层设计*
```

**Critical quality checks:**
- LifecycleStageName union shows all 3 values including "highCommandTick"
- KERNEL_LIFECYCLE_STAGES refactored to `createLifecycleStages(highCommand)` factory function
- `eventBus.clear()` shown as first step of `build()` (prevents handler accumulation)
- `global._lastTick` updated ONLY after `build()` try block succeeds (not before or inside `build()`)
- `_needsBuildFlag = true` shown in EventBus handler (not in `run()` method body)
- No `eventBus.on()` call appears inside `run()` method
- HighCommandEvents uses as const + derived union type pattern
  </action>

  <verify>
    <automated>grep -c "highCommandTick" /Users/lystran/programming/screeps/docs/v2-design/KERN-03-lifecycle.md</automated>
  </verify>

  <acceptance_criteria>
    - File exists: `docs/v2-design/KERN-03-lifecycle.md`
    - `grep "highCommandTick" docs/v2-design/KERN-03-lifecycle.md | wc -l` returns ≥ 3 (union type + KERNEL_LIFECYCLE_STAGES + stage name)
    - `grep "createLifecycleStages" docs/v2-design/KERN-03-lifecycle.md` returns a match (factory function refactor)
    - `grep "HighCommandEvents" docs/v2-design/KERN-03-lifecycle.md` returns a match (event name constants)
    - `grep "eventBus.clear()" docs/v2-design/KERN-03-lifecycle.md` returns a match (first step of build)
    - `grep "_needsBuildFlag" docs/v2-design/KERN-03-lifecycle.md | wc -l` returns ≥ 4
    - `grep "D-1[6-9]" docs/v2-design/KERN-03-lifecycle.md` returns at least 4 matches
    - `grep "Game.notify" docs/v2-design/KERN-03-lifecycle.md | wc -l` returns ≥ 2 (build failure + init/run failure)
    - Document has 10 numbered top-level sections (## 1. through ## 10.)
    - `grep "eventBus.on" docs/v2-design/KERN-03-lifecycle.md | grep "run()" | wc -l` returns 0 (no handler registration in run())
  </acceptance_criteria>

  <done>KERN-03-lifecycle.md exists in docs/v2-design/ with lifecycle.ts before/after comparison showing LifecycleStageName union extension and createLifecycleStages() factory function, complete four-phase execution flows, Build failure recovery pattern, event-driven build mechanism with HighCommandEvents constants, and anti-pattern section — all D-16 through D-19 decisions explicitly referenced.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| EventBus handler lifecycle | Handlers registered in Build must not accumulate across rebuilds |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-06 | Tampering | EventBus handler accumulation | mitigate | Design doc mandates `eventBus.clear()` as first step of `build()`; acceptance criteria checks for this grep |
| T-09-07 | Denial of Service | Build failure infinite retry | mitigate | Build failure path shown with Game.notify() + _needsBuildFlag = true (not infinite loop — Kernel provides outer try/catch) |
</threat_model>

<verification>
After task completes:

```bash
# File exists
ls docs/v2-design/KERN-03-lifecycle.md

# highCommandTick appears multiple times
grep "highCommandTick" docs/v2-design/KERN-03-lifecycle.md | wc -l

# createLifecycleStages factory function
grep "createLifecycleStages" docs/v2-design/KERN-03-lifecycle.md

# eventBus.clear() shown
grep "eventBus.clear()" docs/v2-design/KERN-03-lifecycle.md

# All 4 D-16 through D-19 decisions referenced
grep "D-1[6-9]" docs/v2-design/KERN-03-lifecycle.md
```
</verification>

<success_criteria>
`docs/v2-design/KERN-03-lifecycle.md` is complete with:
- lifecycle.ts before/after showing LifecycleStageName union with 3 members and createLifecycleStages() factory function
- Build phase: 8-step execution sequence, failure recovery (try/catch pattern), EventBus handler registration
- Refresh phase: id→object refresh + rebuildCache(), no structure detection
- Init phase: isolated try/catch per module, no game side effects
- Run phase: isolated try/catch per module, EventBus emit call sites
- HighCommandEvents as const object with 3 event constants (StructureBuilt/RoomClaimed/RoomLost)
- 5 anti-patterns with ❌/✅ blocks
- All D-16, D-17, D-18, D-19 decisions explicitly referenced
</success_criteria>

<output>
After completion, create `.planning/phases/09-kernel-layer-design/09-03-SUMMARY.md`
</output>
